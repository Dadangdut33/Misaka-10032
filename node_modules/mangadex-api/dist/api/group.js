"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.GroupResolver = void 0;
const Agent_1 = require("../Agent");
const base_1 = require("./base");
class GroupResolver extends base_1.ApiBase {
    /**
     * Get a group.
     * @param groupId The group ID number
     * @param options Request options
     */
    async getGroup(groupId, options = {}) {
        const result = await this.agent.callApi(`group/${groupId}`, options);
        return result;
    }
    /**
     * Get a group.
     * @param groupId The group ID number
     * @param options Request options
     */
    static async getGroup(groupId, options = {}) {
        const result = await Agent_1.Agent.callApi(`group/${groupId}`, options);
        return result;
    }
    /**
     * Get partial information about the chapters belonging to the group.
     * @param groupId The group ID number
     * @param options Request options
     */
    async getGroupChapters(groupId, options = {}) {
        const result = await this.agent.callApi(`group/${groupId}/chapters`, options);
        return result;
    }
    /**
     * Get partial information about the chapters belonging to the group.
     * @param groupId The group ID number
     * @param options Request options
     */
    static async getGroupChapters(groupId, options = {}) {
        const result = await Agent_1.Agent.callApi(`group/${groupId}/chapters`, options);
        return result;
    }
}
exports.GroupResolver = GroupResolver;
