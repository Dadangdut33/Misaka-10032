export declare class ApiError extends Error {
    code: number;
    url: string;
    constructor({ message, code, url }: {
        message: string;
        code: number;
        url: string;
    });
}
